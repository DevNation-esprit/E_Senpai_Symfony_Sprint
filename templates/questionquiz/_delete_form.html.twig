<form method="post" action="{{ path('questionquiz_delete', {'id': questionquiz.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ questionquiz.id) }}">
    <button class="btn">Delete</button>
</form>
