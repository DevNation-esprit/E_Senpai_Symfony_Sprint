{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quiz.id }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ quiz.sujet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quiz_index',{'id': app.user.id }) }}">back to list</a>

    <a href="{{ path('quiz_edit', {'id': quiz.id}) }}">edit</a>

    {{ include('quiz/_delete_form.html.twig') }}

    {{ include('questionquiz/index.html.twig') }}
    <a href="{{ path('questionquiz_new',{'id':quiz.id}) }}">Ajouter une Question </a>
{% endblock %}
