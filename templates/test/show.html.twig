{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block body %}
    <h1 style="margin-top: 150px;">Test</h1>
    {{ include('test/_delete_form.html.twig') }}
    <table class="table">
        <tbody>
            <tr>
                <th>Sujet</th>
                <td>{{ test.sujet }}</td>
            </tr>
            <tr>
                <th>NbEtudiantPasses</th>
                <td>{{ test.nbEtudiantPasses }}</td>
            </tr>
            <tr>
                <th>NbEtudiantsAdmis</th>
                <td>{{ test.nbEtudiantsAdmis }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ test.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_index',{'id':app.user.id}) }}">back to list</a>

    <a href="{{ path('test_edit', {'id': test.id}) }}"><i style="font-size:24px;margin-left: 20px;" class="fa fa-pencil-square-o" aria-hidden="true"></i></a>

    {{ include('questiontest/index.html.twig') }}


{% endblock %}
